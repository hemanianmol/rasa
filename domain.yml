version: "3.1"

intents:
  - greet
  - goodbye
  - search_database

responses:
  utter_goodbye:
    - text: "Bye"
  utter_greet:
  - text: "Hey! How are you?"

actions:
  - action_unified_llm

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
